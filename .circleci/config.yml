version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: cd src && g++ -std=c++11  main.cpp -o main && ./main
test:
  override:
    - cd bin; ./gtest_myint
